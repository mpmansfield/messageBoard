<%= form_with(model: user_discipline, local: true) do |form| %>
  <% if user_discipline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_discipline.errors.count, "error") %> prohibited this user_discipline from being saved:</h2>

      <ul>
        <% user_discipline.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
